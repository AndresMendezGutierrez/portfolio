---
import type { TranslateOnlyProps } from "../types";
import Form from "./Form.astro";
import IconComponent from "./IconComponent.astro";
import Toast from "./Toast.astro";

interface Props extends TranslateOnlyProps {}

const { translate } = Astro.props;
---

<header class="mb-14 text-center">
  <h2 id="contact-heading" class="mb-8 text-3.25xl font-semibold tracking-tight">
    {translate("contact_title")}
  </h2>

  <menu aria-label={translate("contact_label_menu")} class="flex justify-center gap-6">
    <li>
      <a
        href="https://linkedin.com/"
        target="_blank"
        rel="noopener noreferrer"
        aria-label={translate("contact_label_linkedin")}
      >
        <IconComponent icon="linkedin-icon" class="h-16 w-16 transition-transform hover:scale-110 hover:text-primary" />
      </a>
    </li>
    <li>
      <a href="tel:+34910009718" aria-label={translate("contact_label_phone")}>
        <IconComponent icon="phone-icon" class="h-16 w-16 transition-transform hover:scale-110 hover:text-primary" />
      </a>
    </li>
    <li>
      <a
        href="https://github.com/AndresMendezGutierrez"
        target="_blank"
        rel="noopener noreferrer"
        aria-label={translate("contact_label_github")}
      >
        <IconComponent icon="github-icon" class="h-16 w-16 transition-transform hover:scale-110 hover:text-primary" />
      </a>
    </li>
  </menu>
</header>

<section class="mb-12 text-center">
  <p class="text-3.25xl font-semibold" role="presentation">
    {translate("contact_or")}
  </p>
  <p class="mt-6.5 text-xl/5.5">
    {translate("contact_email_instruction")}
  </p>
</section>

<Toast translate={translate} />
<Form translate={translate} />
